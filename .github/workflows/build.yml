name: bluebuild
on:
  schedule:
    - cron:
        "00 06 * * *" # build at 06:00 UTC every day
        # (20 minutes after last ublue images start building)
  push:
    paths-ignore: # don't rebuild if only documentation has changed
      - "**.md"

  pull_request:
  workflow_dispatch: # allow manually triggering builds
concurrency:
  # only run one build at a time
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true
jobs:
  bluebuild:
    name: Build paradise
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    strategy:
      fail-fast: false # stop GH from cancelling all matrix builds if one fails
      matrix:
        recipe:
          # !! Add your recipes here
          - paradise.yml
          - paradise-nvidia.yml
          - paradise-macos.yml
    steps:
      # copied from wayblueorg/wayblue repo
      - name: Optimize build times
        shell: bash
        run: |
          echo 'set man-db/auto-update false' | sudo debconf-communicate
          sudo dpkg-reconfigure man-db
          sudo rm -f /var/lib/man-db/auto-update
          sudo sed -i 's/^update_initramfs=.*/update_initramfs=no/' /etc/initramfs-tools/update-initramfs.conf
      - name: Checkout repo
        uses: actions/checkout@v6.0.2
        with:
          persist-credentials: false
      - name: Gather image data from recipe
        run: |
          echo "IMAGE_NAME=$(grep '^name:' ./recipes/${{ matrix.recipe }} | sed 's/^name: //')" >> $GITHUB_ENV
          echo "IMAGE_MAJOR_VERSION=$(grep '^image-version:' ./recipes/${{ matrix.recipe }} | sed 's/^image-version: //')" >> $GITHUB_ENV
          BASE_IMAGE=$(grep '^base-image:' ./recipes/${{ matrix.recipe }} | sed 's/^base-image: //')
          echo "BASE_IMAGE_NAME=$(echo $BASE_IMAGE | sed 's/.*\/.*\///')" >> $GITHUB_ENV
      - name: Verify base image
        uses: EyeCantCU/cosign-action/verify@v0.3.0 # v0.3.0
        with:
          containers: ${{ env.BASE_IMAGE_NAME }}:${{ env.IMAGE_MAJOR_VERSION }}
          registry: "ghcr.io/wayblueorg"
          pubkey: "https://raw.githubusercontent.com/wayblueorg/wayblue/refs/heads/live/cosign.pub"
      # the build is fully handled by the reusable github action
      - name: Build paradise image
        uses: blue-build/github-action@v1.11.1
        with:
          recipe: ${{ matrix.recipe }}
          cosign_private_key: ${{ secrets.SIGNING_SECRET }}
          registry_token: ${{ github.token }}
          pr_event_number: ${{ github.event.number }}
          skip_checkout: true
          retry_push_count: 3
          verify_install: true
          # Those two parameter seems make iso not installable due to same sha256 for layers
          # like this issue: https://github.com/ostreedev/ostree-rs-ext/issues/592
          # build_chunked_oci: true
          # squash: true
      - name: Get username lowercase
        id: username
        uses: ASzc/change-string-case-action@v8
        with:
          string: ${{ github.repository_owner }}
      - name: Get image name lowercase
        id: imagename
        uses: ASzc/change-string-case-action@v8
        with:
          string: ${{ env.IMAGE_NAME }}
      - name: Build paradise ISO
        uses: jasonn3/build-container-installer@v1.4.0
        id: iso
        with:
          arch: x86_64 # wait for wayblue to add arm64 support...
          image_repo: ghcr.io/${{ steps.username.outputs.lowercase }}
          image_name: ${{ steps.imagename.outputs.lowercase }}
          image_tag: ${{ env.IMAGE_MAJOR_VERSION }}
          version: ${{ env.IMAGE_MAJOR_VERSION }} # fedora version
          iso_name: ${{ env.IMAGE_NAME }}-${{ env.IMAGE_MAJOR_VERSION }}.iso
          enrollment_password: paradise
      - name: Upload ISO as artifact
        id: iso-upload
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.iso.outputs.iso_name }}
          path: |
            ${{ steps.iso.outputs.iso_path }}/${{ steps.iso.outputs.iso_name }}
            ${{ steps.iso.outputs.iso_path }}/${{ steps.iso.outputs.iso_name }}-CHECKSUM
          if-no-files-found: error
          retention-days: 90
          compression-level: 0
